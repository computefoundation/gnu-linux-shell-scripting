
# Get the maximum backlight level.
# The interface to the ACPI kernel module for video (e.g. intel_backlight) can be found with "ls /sys/class/backlight/".
cat /sys/class/backlight/intel_backlight/max_brightness

# Set the backlight level.
# The interface to the ACPI kernel module for video (e.g. intel_backlight) can be found with "ls /sys/class/backlight/".
tee /sys/class/backlight/intel_backlight/brightness <<< <backlight level>

